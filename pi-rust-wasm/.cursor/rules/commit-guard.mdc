---
alwaysApply: true
---

# Cursor 提交规范 + 覆盖率自动校验规则（Rust 专用）

**作用域**：Git 提交 / 代码 Commit 场景  
**生效范围**：当前 Rust 项目全量 Agent

---

## 一、提交前强制自动执行（所有代码提交）

1. **执行单元测试**

   ```bash
   cargo test --all
   ```

   - 测试不通过：禁止提交，必须修复后重试

2. **自动计算代码覆盖率（仅行覆盖率）**

   ```bash
   cargo tarpaulin --out stdout --no-fail-fast --quiet 2>/dev/null | grep -oE '[0-9]+\.[0-9]+%' | head -n1
   ```

   - 将结果保存为变量：`COVERAGE=结果`
   - 覆盖率为空 / 计算失败：禁止提交

---

## 二、提交信息（Commit Message）强制格式

### 1. 常规代码提交（修改 src/ 代码、Cargo.toml、测试文件等）

必须严格遵循（与 Constitution 及规范一致）：**格式与 what+why 要求见 [Commit Message 规范](../openspec/specs/guides/COMMIT_MESSAGE_SPEC.md)**。摘要如下：

- 首行：`类型(模块): 简短描述(what)`，不超过 50 字。
- 详细描述（why）：说明**为什么这么做**——动机、要解决的问题、改动的作用与意义；禁止记流水账。
- 末尾：`[cov = xx.x%]`，覆盖率与 tarpaulin 实测一致。

### 2. 禁止行为

- 禁止无格式提交
- 禁止手动编造覆盖率数字
- 禁止删除 `[cov = xx.x%]` 字段
- 禁止提交信息为空、过短（< 5 字）
- 标题行不得超过 50 字

---

## 三、例外豁免规则（满足则不要求覆盖率）

**仅当本次提交唯一修改的文件均在 `status/*.md` 内，或唯一修改文件为 `INTEGRATION.md` 时**：

- 豁免：不执行测试
- 豁免：不计算覆盖率、不写 `[cov=...]`
- 仍必须遵守提交格式：
  - 仅修改 status 时示例：
    ```
    chore(doc): 更新 status/feature-xx.md 进度/阻塞说明

    用于阻塞上报、进度同步。

    （豁免时不写 [cov = xx.x%]）
    ```
  - 仅修改 INTEGRATION 时（由汇总 command 使用）：
    ```
    chore(doc): 更新 INTEGRATION.md 进度/阻塞说明

    用于看板汇总。

    （豁免时不写 [cov = xx.x%]）
    ```

**用途**：开发分支只改 status 碎片；INTEGRATION.md 仅在 develop 上由汇总 command 更新；两种情形均不要求当次提交跑全量测试/覆盖率。

---

## 四、提交前自动检查逻辑（Agent 内部校验）

在执行 `git commit` 前，Agent 必须自动做以下检查：

1. **检查本次 Git 修改文件列表**
   - 若修改仅包含：`status/*.md` 内文件 → 走豁免规则
   - 若修改仅包含：`INTEGRATION.md` → 走豁免规则
   - 若包含任何代码 / 配置 / 测试文件或其他文件 → 走常规规则

2. **检查 Commit Message 格式**
   - 首行为「类型(模块): 简短描述」，类型仅限 feat/fix/docs/style/refactor/test/chore，描述不超过 50 字
   - 可含详细描述（why）空行后书写：**必须写清「为什么这么做」——动机、要解决的问题、以及改动带来的作用与意义；禁止只罗列改了哪些文件（流水账）**
   - 常规提交必须包含 `[cov = xx.x%]`（等号两侧留空格），覆盖率数值与刚刚计算的 `COVERAGE` 完全一致
   - 格式 / 数值不匹配 → 立即拒绝提交，并重写正确 message

---

## 五、Agent 固定执行流程（必须严格遵守）

1. 询问：本次提交修改了哪些文件？
2. 判断是否仅修改 `status/*.md` 或仅修改 `INTEGRATION.md`
   - **是** → 按豁免规则生成合规 commit
   - **否** → 自动跑 test → 自动算 coverage → 生成带覆盖率标准 commit
3. 校验通过 → 允许提交；不通过 → 终止并提示原因
