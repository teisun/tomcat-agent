### T2-P0-006 | thinking-api-and-display | Thinking/Reasoning 协议 + EventBus + CLI 展示（TUI 复用基座）

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **状态** | `DONE`（`2026-05-08` Nibbles 合并入 `develop` @ `8c7b86e`，`run-integration-tests.sh all` EXIT_CODE=0） |
| **负责人** | Tom |
| **分支** | `feature/thinking-api-display` |
| **阻塞点** | — |
| **架构方案（执行顺序与验收以本文为准）** | [llm-stream-events-cli-pipeline.md](../../docs/architecture/llm-stream-events-cli-pipeline.md) |
| **关联调研** | [llm-tool-rounds-cli-display-thinking-protocol.md](../../docs/reports/llm-tool-rounds-cli-display-thinking-protocol.md) |

**目标**：按架构文档 **§4** 定稿，打通 **Provider → `StreamEvent::Thinking` → `stream_handler` → EventBus `message_update`（`kind=thinking_delta`）→ CLI**；默认不改变现有用户行为（`llm.thinking.enabled=false` 等，见方案 **§3.1 G5**、**§9**）。全屏 TUI 面板与 **Ctrl+T** 仍归 **T2-P0-008**；本卡交付 **CLI** 侧报告式输出与 **`/thinking`** 折叠（方案 **§4.2.3–§4.2.4**）。

**实施顺序（与架构 §4.2 一致）**：

| 代号 | 交付 | 主要落点（见架构 §4.2 表） |
|------|------|---------------------------|
| **P1** | `StreamEvent::Thinking { delta, signature }` + serde | `src/core/llm/types.rs` |
| **P2a** | Completions：`reasoning_content` 解析 + 请求侧 `reasoning_effort` / `thinking`（按 `thinking_format`） | `src/core/llm/openai.rs` |
| **P2b** | Responses：`response.reasoning_*` SSE → `StreamEvent::Thinking`（替换 `_ => ignore`） | `src/core/llm/openai_responses/stream.rs` |
| **P3** | `stream_handler` 透传 → `MessageUpdate` 带 `kind=thinking_delta` | `src/core/agent_loop/stream_handler.rs`、`src/infra/events/mod.rs` |
| **P0** | `CliTurnRenderer`：`[thinking]` / `[tool]` / 正文交错 + ANSI（dim 优先） | `src/api/chat/cli_turn_renderer.rs`（新）、`src/api/chat/mod.rs` |
| **P4** | 折叠：`show_thinking` + 命令 `/thinking`（子命令 `on` / `off` / `toggle`）+ 环境变量 `PI_CHAT_SHOW_THINKING` | `src/api/chat/commands/*`、renderer 或 `ChatContext` 状态 |
| **P5** | `ThinkingLevel` + `thinking_format` + `map_reasoning_effort`（对齐 pi-mono 档位语义） | `src/infra/config.rs`、`src/core/llm/thinking_policy.rs`（新） |
| **P6** | 多轮：`strip_thinking_on_resend` + 厂商策略表（DeepSeek strip 等） | `src/core/session/...` 或 agent 组装请求前钩子 |
| **P7** | 持久化：`llm.thinking.persist` + audit；默认不落 transcript | transcript / audit 路径 |

**子项（勾选即 PR 粒度）**：

- [x] **P1** `StreamEvent::Thinking`，单测 `stream_event_thinking_serde`（命名以架构 **§5.1** 为准）
- [x] **P2a** OpenAI Chat Completions：`reasoning_content` 三路兼容思路（架构 **§4.2.1** Inbound 表）+ 请求体扩展
- [x] **P2b** OpenAI Responses：reasoning 相关 SSE 显式映射 + 未知事件 `tracing::debug!`（架构 **§4.2.1**）
- [x] **P3** `stream_handler` 发 `message_update`，载荷含 `kind`：`content_delta` / `thinking_delta`（架构 **§5.2**）
- [x] **P0** `CliTurnRenderer`：对齐报告 **§2.2** 与架构 **§4.2.3**（工具 `one_line_summary`、ANSI dim+gray、stderr 装饰避免与 prompt 打架）
- [x] **P4** 方案 D：架构 **§4.2.4**（CLI 无 Ctrl+T 时用命令行 `/thinking`；TUI 的 Ctrl+T 留给 T2-P0-008）
- [x] **P5** 配置：`llm.thinking.enabled` / `.level` / `.format` / `.max_tokens` / `.show` / `.persist` / `.strip_on_resend` / `.print_to_stderr`；`thinking_policy::resolve_request_fields` 集中映射
- [x] **P6** 多轮重发策略：`should_strip_on_resend` + `strip_anthropic_thinking_blocks`（DeepSeek/Anthropic 形状策略均可测）
- [x] **P7** 默认 thinking 不进 transcript；`should_persist_thinking` 双开关防孤儿（架构 **§4.1 R8**）
- [ ] **Anthropic / Qwen**：本卡保留字段形状与策略 helper，正式接通 chat_stream 拆后续任务（与任务标题「多厂商」一致）

**依赖**：T2-P0-001（`stream_handler` 落位后再接 **P3** 最顺）

**被依赖**：T2-P0-008（全屏 TUI：`showThinking` + **Ctrl+T**、历史面板等消费同一 EventBus 载荷）

**协作接口**：

- **消费**：`LlmProvider::chat_stream` → `StreamEvent` 流
- **对外可观测**：EventBus `message_update`（`kind` 区分正文 / thinking）、`tool_execution_*`；CLI 仅 **`CliTurnRenderer` 单订阅者**写终端（架构 **§4.1 R1**）

**验收标准**（对齐架构 **§3.1** 与 **§11**）：

- **G3 / G5**：`thinking.enabled=false` 时行为与当前主线一致；`true` 时 OpenAI **Completions 或 Responses** 至少一条路径可稳定产出 `thinking_delta`（架构 G3）
- **G1**：CLI 出现报告式 **`[thinking]` + `[tool]` + 正文** 交错与灰/绿/红语义（架构 **§4.2.3**）
- **G2**：`/thinking` 与 `show_thinking` 折叠行为符合架构 **§4.2.4**
- **G4**：改 `ThinkingLevel` / `thinking_format` 后下一请求 wire 体字段正确（单测或快照测）
- **单元 / 集成**：架构 **§11** 所列用例至少覆盖：`openai` chunk、`openai_responses` stream、`stream_handler` 顺序、`CliTurnRenderer` 折叠一行、chat `/thinking` + 工具行

---
