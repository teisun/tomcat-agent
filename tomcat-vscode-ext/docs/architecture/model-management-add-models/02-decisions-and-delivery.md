# 模型管理与 Add Models · 02 落地选型与实施

> 总览见 [`../model-management-add-models.md`](../model-management-add-models.md)（含定位、阅读顺序与文首导图集）。
> 本文对应 [`ARCHITECTURE_SPEC.md`](../../../../tomcat/docs/openspec/specs/guides/workflow/ARCHITECTURE_SPEC.md) 的 **§3 落地选型与实施（已定稿）**。
> 上游调研与术语见 [`01-scope-and-research.md`](01-scope-and-research.md)；协议 / One-Glance / 验证与风险见后续子文档。

---

## 3. 落地选型与实施（已定稿）（MUST）

### 3.0 章节编排

总览文档 `../model-management-add-models.md` 的 `## 1`–`## 10` 与 ARCHITECTURE_SPEC §1–§10 语义一一对应；文首导读与附录 A（UI 图集）不占用 § 编号。本文承接其中的 §3，保留 §3.1 决策表 + §3.2 实施点相邻，reviewer 先读决策再对落地。

### 3.1 落地选型决策表（MUST）

| 维度              | 关切                   | 决策                                                           | 取自                                                                                                                                                                                             | 入选理由                                                                                           | 未入选 + 拒因                                                       | 说人话                                       |
| --------------- | -------------------- | ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | ----------------------------------------- |
| D1 设置页承载        | 两栏布局放哪               | 采用编辑器区全屏 WebviewPanel；拒绝 EditorPane / CustomEditor / 侧栏 overlay | `vscode/` Settings UI 用 `EditorPane`（workbench 内部，非扩展 API）；本仓 `[ui/webview/provider.ts](../../../src/ui/webview/provider.ts)`（`WebviewViewProvider`，证明我们是插件、够不着 EditorPane）                                                                  | 设计：`SettingsPanel.ts` 新建 editor-area `WebviewPanel`；理由：插件可用、无需绑定文件、与侧栏 webview 同栈可复用 CSP/nonce/主题变量/`tc-*`，宽度够放左导航+右内容                         | EditorPane：拒因=workbench 内部类、扩展 API 未导出、插件不可用；CustomEditor：拒因=必须绑定 document/URI，为「无文件配置页」伪造假文件属过度设计；侧栏 overlay：拒因=侧栏宽度不足、会挤占聊天区                                    | 设置页做成独立标签页；EditorPane 是 VS Code 内核专用够不着，CustomEditor 要挂个文件没必要，WebviewPanel 正好。                     |
| D2 写入路径         | 谁来改 models.toml/.env | 采用新增 serve 模型管理命令 + 单一 `core/llm/admin.rs`；拒绝宿主 TS 直写        | 本仓 `[catalog.rs](../../../../tomcat/src/core/llm/catalog.rs)`（唯一 merge 实现）；goose `crates/goose/src/providers/declarative/*`（provider 逻辑集中在后端）                                               | 设计：CLI/serve 都转调 `admin.rs`；理由：单一事实源、CLI 天然共享、Key 安全内聚                                         | TS 直写文件：拒因=两套解析/校验易漂移、Key 暴露面变大、Rust 进程不自知                     | 写盘只写一份 Rust，命令行和界面都用它。                    |
| D3 下拉形态         | 原生 select 够不够        | 采用自定义下拉；拒绝原生 `<select>`                                      | 本仓 `[Composer.tsx:690](../../../gui/src/components/Composer.tsx)`（现为原生 select）；`[SessionBar.tsx](../../../gui/src/components/SessionBar.tsx)`（已有自定义下拉范式）；Cursor 附件2                            | 设计：仿 SessionBar 的 open/click-outside/Esc；理由：原生 select 无法渲染分隔符与「Add Models」页脚                   | 原生 select：拒因=不支持分隔符/页脚动作/富行                                    | 原生下拉画不出分隔线和「加模型」，得自己写一个。                  |
| D4 内置表结构        | 内置模型放几张表             | 采用单一内嵌 `builtin_models.toml` 数据源 + `init` 原样释放 seed 到 `models.toml`；拒绝手写 `MANAGED_MODELS` 双表       | 本仓 `[builtin_models.toml](../../../../tomcat/src/core/llm/builtin_models.toml)`；`[catalog.rs](../../../../tomcat/src/core/llm/catalog.rs)` 解析逻辑；`[models_toml.rs](../../../../tomcat/src/api/cli/models_toml.rs)` seed 逻辑                            | 设计：运行时只认内嵌单源，一切 seed 文本都直接来自同一份内嵌文件；理由：开机即在、单一事实源、用户又能在 `models.toml` 里直接看 / 改 / 删                                | 保留手写双表：拒因=两处真相、init 未跑则缺模型、易不一致                                  | 运行时只认一份编进二进制的模型清单，init 会把同一份预置原样写进 models.toml 给人改。                |
| D5 GLM endpoint | base_url 非 /v1 怎么办   | 采用 path-aware endpoint 构造；拒绝破坏性改 base_url 语义；备选=显式 wire_path | 本仓 `[openai.rs:615](../../../../tomcat/src/core/llm/openai.rs)`（硬编码 `{base}/v1/chat/completions`）；goose PR#8204（zhipu 用 `/api/paas/v4`）                                                     | 设计：base 无路径→`/v1/<leaf>`，带路径→`/<leaf>`；理由：向后兼容 bare host，同时让 GLM 的 `/api/paas/v4` 生效           | 破坏性改语义（要求 base 带 /v1）：拒因=打断现有 models.toml；显式 wire_path：留作边界安全阀 | 智谱地址不是 /v1，用「地址带不带路径」自动判断拼哪种后缀。           |
| D6 Anthropic 接入 | 走兼容网关还是新 wire        | 采用新 `anthropic-messages` provider；拒绝 OpenAI 兼容网关代理           | 本仓 `[registry.rs](../../../../tomcat/src/core/llm/registry.rs)`（api→provider 分派）；Anthropic 官方 `/v1/messages` 文档                                                                             | 设计：新建 `core/llm/anthropic/*` + registry 加一行；理由：Opus 官方无一等 OpenAI wire，thinking/signature 需原生支持 | 兼容网关：拒因=依赖第三方代理、丢失原生 thinking/signature 保真                     | Claude 不是 OpenAI 协议，老老实实写一套 /v1/messages。 |
| D7 目录运行时生效      | 加完模型要不要重启            | 采用可重载 catalog 句柄；拒绝重启 serve                                  | 本仓 `[session_runtime.rs](../../../../tomcat/src/api/chat/session_runtime.rs)` `GlobalServices.model_catalog: Arc<ModelCatalog>`；`[context.rs:352](../../../../tomcat/src/api/chat/context.rs)` | 设计：改 `RwLock<Arc<ModelCatalog>>`/arc-swap，写盘后 reload，resolver 读当前值；理由：加模型即时可用、不断会话             | 重启 serve：拒因=打断所有会话、体验差                                         | 加完模型立刻能用，不用重开后端。                          |
| D8 Key 存储/保护    | 密钥怎么存、怎么防泄露          | 采用 `.env`(0600) + 密文只入不出 + 脱敏；拒绝明文入协议/回显                     | 本仓 `[init_model_wizard.rs](../../../../tomcat/src/api/cli/init_model_wizard.rs)`（init 已写 .env 0600）；`[auth.rs](../../../../tomcat/src/core/llm/auth.rs)`（按 env 解析凭证）                           | 设计：`set_provider_key` value 只入不出、响应仅 `key_present`、日志脱敏、input type=password；理由：与 init 一致、暴露面最小 | 明文入 list/回显：拒因=webview/日志泄漏密钥                                  | 钥匙落 .env 且 600 权限，界面永不回显、日志打码。            |

### 3.2 实施点（已闭环）（MUST）

| 实施点                     | 交付范围（含交付物）                                                    | 主要代码落点（含落地点）                                                                                                                                                                                                                                                         | 验收锚点（示例）                                                          | 说人话                  |
| ----------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------- | -------------------- |
| IP0 契约与文档               | 本架构文档 + ModelView/新命令跨层契约草案                                   | 本文；`[serve/types.rs](../../../../tomcat/src/api/serve/types.rs)` 草案；`[wire.d.ts](../../../src/serveClient/wire.d.ts)`/`[protocol.ts](../../../src/serveClient/protocol.ts)` 草案                                                                                           | 文档定稿；`--print-schema` 产出新命令类型                                     | 先把「界面/后端说同一种话」钉死。    |
| IP1a Rust 写盘中枢          | `admin.rs`（list/upsert/remove/set_key/list_keys）+ 可重载 catalog | 新建 `core/llm/admin.rs`；改 `[session_runtime.rs](../../../../tomcat/src/api/chat/session_runtime.rs)`/`[resolver.rs](../../../../tomcat/src/core/llm/resolver.rs)`                                                                                                      | `core/llm/tests/admin_test.rs` + `core/llm/tests/resolver_test.rs` | 加一个「唯一写盘 + 可热重载」的后端。 |
| IP1b 内置预置 + path-aware  | 内嵌 `builtin_models.toml` 补齐 9+ 模型；endpoint path-aware                 | 新增 `[builtin_models.toml](../../../../tomcat/src/core/llm/builtin_models.toml)`；改 `[catalog.rs](../../../../tomcat/src/core/llm/catalog.rs)` 解析内嵌预置 / `infer_default_base_url()`；改 `[openai.rs](../../../../tomcat/src/core/llm/openai.rs)`/openai_responses                                                                                | `catalog::tests::*` + `core/llm/endpoint.rs::tests`                | 出厂内置常用模型；修好智谱地址拼接。   |
| IP1c Anthropic provider | `anthropic-messages` wire（消息/SSE/thinking）                    | 新建 `core/llm/anthropic/`；改 `[registry.rs](../../../../tomcat/src/core/llm/registry.rs)`/`[thinking_policy.rs](../../../../tomcat/src/core/llm/thinking_policy.rs)`/`[replay_policy.rs](../../../../tomcat/src/core/llm/replay_policy.rs)`                                | `thinking_policy_test` + `replay_policy_test` + `openai_stream_test` | 把 Claude 接进来。        |
| IP1d serve + CLI 门面     | 新 serve 命令/capabilities；`tomcat model` 子命令                    | 改 `[serve/types.rs](../../../../tomcat/src/api/serve/types.rs)`/`[commands.rs](../../../../tomcat/src/api/serve/commands.rs)`/`[control.rs](../../../../tomcat/src/api/serve/control.rs)`；新建 `cli/model_cmd.rs`；改 `[cli/mod.rs](../../../../tomcat/src/api/cli/mod.rs)`     | `commands_test` + `control_test` + `model_cmd_test` + `nested_guard_test` | 给后端两个门面：命令行 + serve。 |
| IP2 扩展宿主                | serveClient 新方法 + SettingsPanel + intents                     | 改 `[TomcatMessenger.ts](../../../src/serveClient/TomcatMessenger.ts)`/`[initialize.ts](../../../src/serveClient/initialize.ts)`/`[ui/webview/provider.ts](../../../src/ui/webview/provider.ts)`；新建 `ui/settings/SettingsPanel.ts`；改 `[package.json](../../../package.json)` | `src/ui/webview/tests/provider.test.ts` + `tests/webview_provider_flow.test.ts` | 宿主把命令接上、开出设置标签页。     |
| IP3 GUI                 | 自定义下拉 + 设置中心壳 + Models 页（含密文 Key）                             | 改 `[Composer.tsx](../../../gui/src/components/Composer.tsx)`/`[gui/src/types.ts](../../../gui/src/types.ts)`/`[vite.config.ts](../../../gui/vite.config.ts)`；新建 `gui/src/settings/*`                                                                                     | `Composer.test.tsx` + `SettingsApp.test.tsx`                        | 把界面做出来。              |
| IP4 测试 + 收尾文档           | 单元/集成/E2E/安全用例；models.toml/user-guide/README                  | `[e2eHostFixture.ts](../../../scripts/e2eHostFixture.ts)`；`[user-guide.md](../../../../tomcat/docs/user-guide.md)`                                                                                                                                                    | 见 [`04-operations-validation-history.md`](04-operations-validation-history.md) 的 §8 | 用测试钉死行为、更新文档。        |

> IP0→IP4 与开发计划 Phase 0→4 一一对应；验收锚点现在直接指向已落地的测试文件，后续增量只继续补测试，不再靠 `PENDING` 占位。
