### T2-P1-020 | Tomcat VSCode Chat 扩展 Phase 2

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `DONE` |
| **负责人** | Tom |
| **分支** | `feature/tomcat-vscode-extension` |
| **阻塞点** | —（2026-07-05 已由 Nibbles 在 `develop` 本地完成合并验收；自动化门禁、安装 E2E、`verify:vsix` 视觉截图与文档回写均已完成） |
| **SSoT** | [`tomcat-vscode-extension-phase2.md`](../../../../tomcat-vscode-ext/docs/architecture/tomcat-vscode-extension-phase2.md)（唯一真相；任务卡不重复展开） |

### 本轮完成

- Stage A `/plan` / `/model` slash 与 `tomcat serve` 协议扩展已完成，schema / d.ts fixture 已刷新。
- Stage B sidebar webview 已重构为聊天式 React UI：timeline transcript、thinking/tool/approval/plan 文件卡片、活动 plan strip、附件 chips、底部 composer 工具条（`+` / `Chat|Plan` / `Model` / `Ctx%` / 发送）。
- 历史补齐、plan 文件打开、附件 base64 透传、context budget 展示、共享会话池与单活跃归属均已接通。
- 本地门禁已完成：`npm run build`、`npm run test:unit`、`npm run test:integration`、`npm --prefix gui run test`。
- 真实桌面 UI 验收已完成：真实打开 Tomcat 侧栏并执行发送消息、模型切换、`Chat <-> Plan`、`Build`、plan 文件打开、附件添加/发送与 `Ctx%` 观察。
