### T2-P1-009 | bash AST `detect_unsupported` 精度与误伤治理

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `TODO` |
| **负责人** | — |
| **分支** | `feature/bash-ast-detect-precision`（建议；认领时自定） |
| **阻塞点** | — |
| **开发计划** | [bash_detect_unsupported_对比_ac5d829c.plan.md](/Users/yankeben/.cursor/plans/bash_detect_unsupported_对比_ac5d829c.plan.md)（`~/.cursor/plans/...`；**SSoT**：意图表、cc-fork 对标、减误伤方案 §4、`todos` frontmatter） |
| **来源** | 2026-05-06：生产默认 **`BashAstChecker.enabled = false`**（`DefaultPrimitiveExecutor::new`、`ToolsBashAstConfig::default`），解析精度到位后再由配置或 `with_bash_ast` 打开。 |

**目标**：按关联计划推进——降低 [`bash_ast::detect_unsupported`](../../../src/core/permission/bash_ast.rs) 与手写切段的 **粗匹配误伤**；可选：句首/操作符后关键字、真 heredoc、`enabled` 与 denylist 绑定、**tree-sitter-bash** 等。契约对齐 [bash-pr-l-scope.md](../../../architecture/tools/bash-pr-l-scope.md) §4、[bash.md](../../../architecture/tools/bash.md) §10 T3。

**子项**（与计划 §4 / frontmatter `todos` 对齐；认领时勾选或拆 PR）：

- [ ] **harden-parse**：实现选型 + 代码 + 回归（`detect_unsupported` / `split_segments` / `suite_test` 三条 `bash_ast_*` 保持显式 `enabled=true`）
- [ ] **doc-fp**（可选）：`bash-pr-l-scope.md` / `bash.md` 补 FP 范围与 cc-fork 差异说明
- [ ] **`[tools.bash.ast]`**：TOML 反序列化 + `api/chat`（或等价装配）注入 `enabled` / allow / deny（可与上两项同 PR 或拆分）

**依赖**：T2-P0-016 bash PR-L 骨架（`bash_ast.rs`、`with_bash_ast`、`execute_bash_impl` 调用顺序）。

**被依赖**：—（可与 `[tools.bash.ast]` 装配同批）

**协作接口**：`BashAstChecker`、`split_segments`、`detect_unsupported`、`DefaultPrimitiveExecutor::bash_ast`、后续 config 装配。

**验收标准**：

- `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test --lib` 全绿；相关集成测不回归
- 关联计划 frontmatter **`todos`**（`doc-fp`、`harden-parse`）各有 **PR 落地** 或 **书面放弃/推迟** 记录在本任务讨论或 PR 描述中

---

**已落地备忘（实施本卡前勿删，便于对照）**：当前默认 **`enabled=false`**，不切段、不跑 `detect_unsupported`；单测里需要 AST 行为须 `.with_bash_ast(BashAstChecker::new(true, …))`。与计划 §3 历史表述冲突时以 **`src/` + 本段** 为准。
