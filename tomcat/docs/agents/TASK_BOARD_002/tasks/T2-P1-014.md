### T2-P1-014 | Skill 系统

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `PENDING_INTEGRATION` |
| **负责人** | Spike |
| **分支** | `feature/skill-system` |
| **阻塞点** | — |
| **SSoT** | [`skill-system.md`](../../../architecture/skill-system.md)（§4.1 决策表、§4.2 路线图、§5 协议、§11 测试）；冲突以该文档 + `src/` 为准 |
| **来源** | Product_Brief P2；架构文档已定稿；2026-06-06 按用户显式指派提前执行，落地仍以架构文档为准 |

**目标**：P0→P2 三层根发现 `<name>/SKILL.md` → `<available_skills>` 仅注入 name+description → 模型 `load_skill(name)` 按名装载正文；技能零特权，正文动作仍过 `PermissionGate`。

**子项**（§4.2 顺序；**PR-SK-B 下期不做**）：

- [x] **PR-SK-A**：`Skill` / `SkillFrontmatter` / `SkillsConfig`（`core/skill` 骨架 + `AppConfig.skills`）
- [x] **PR-SK-D**：`skill_roots()` 三层扫描、first-wins、`SkillSet`（后台 spawn，首轮 prompt 前 await）
- [x] **PR-SK-P**：`AvailableSkillsSection` + 预算 A+D 降级（截描述 → name-only）
- [x] **PR-SK-T**：`load_skill` catalog / `tool_exec` / guard（reviewer/verifier 默认拒）
- [x] **PR-SK-C**：`/skill list|reload|use` + `tomcat skill list|reload`

**依赖**：无开放 T2 硬阻塞；热区 `tool_exec/`、`system_prompt/`（邻接 T2-P1-012/013），认领前同步 `develop`。

**协作接口**：`core/skill/*`、`system_prompt.rs`、`contract/catalog.rs`、`tool_exec/`、`permission/gate.rs`、`api/chat/commands`、`api/cli`。

**验收**：§11 锚点（frontmatter 解析、三层发现去重、prompt 仅元数据、load_skill 按名/guard/gate、配置 TOML/env）已落地；工程师分支侧已跑通 `cargo fmt --all`、`cargo clippy --all-targets -- -D warnings`、`cargo test --lib -- --nocapture` 与 `./scripts/run-integration-tests.sh all`，当前进入 `PENDING_INTEGRATION`。
