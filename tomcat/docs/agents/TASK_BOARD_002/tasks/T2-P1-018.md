### T2-P1-018 | Agent Server 与 UI Gateway

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `DONE` |
| **负责人** | Spike |
| **分支** | `feature/agent-server-ui-gateway` |
| **阻塞点** | — |
| **SSoT** | [`agent-server-and-ui-gateway.md`](../../../architecture/agent-server-and-ui-gateway.md)（唯一真相；任务卡不重复展开） |

**develop 侧验收**：已本地快进合并到 `develop`（feature 收尾提交 `99a720f`），并按以下口径复跑通过：

- clean env `./scripts/run-integration-tests.sh all`
- `set -a && source .env && set +a && NO_PROXY=<gateway-host> no_proxy=<gateway-host> ./scripts/run-integration-tests.sh integration-real-llm`
- `set -a && source .env && set +a && NO_PROXY=<gateway-host> no_proxy=<gateway-host> ./scripts/run-integration-tests.sh integration-openai-responses-wire`

**live 备注**：本机接入内网时，私有网关 host 应加入 `NO_PROXY` 直连，避免本地 `127.0.0.1:7890` 代理链路把 `responses_inline_image_describe_roundtrip` 打成 HTML `403 Forbidden`。同口径下 `responses_inline_pdf_input_file_summarize_roundtrip` 记录过一次瞬时“未看到附件”的 live 抖动，但原文件级复跑与脚本重试均已转绿。`PI_LIVE_OPENAI_FILES=1` 相关 OpenAI Files 真链路仍保持显式 opt-in，不纳入默认门禁。
