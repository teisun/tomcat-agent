### T2-P1-013 | `web_fetch` 工具

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `TODO` |
| **负责人** | — |
| **分支** | `feature/web-fetch`（建议；认领时自定） |
| **阻塞点** | — |
| **SSoT** | [`web_fetch.md`](../../../architecture/tools/web_fetch.md)（§2.4 路线图、§4 协议、§10 测试）；冲突以该文档 + `src/` 为准 |
| **来源** | 002 迭代：补齐 URL 抓取；与 [`web_search.md`](../../../architecture/tools/web_search.md) 双工具分卡 |

**目标**：模型 `url` → Markdown（或二进制落盘返路径）；reqwest GET + 域名权限 + 受控重定向；**不做**检索（归 T2-P1-012）。

**子项**（顺序固定；**PR-WF-P 本期不做**，见 SSoT §2.4）：

- [ ] **PR-WF-A**：catalog 3 字段 schema、`tool_exec/branches/web_fetch.rs` 占位、system_prompt
- [ ] **PR-WF-S**：reqwest + `Policy::none()` 重定向循环、HTML→Markdown、`ToolsWebFetchConfig`、`moka` + `html2md`
- [ ] **PR-WF-D**：`PermissionGate::Domain` + `check_domain`、三档域名配置、审计 scope
- [ ] **PR-WF-B**：PDF/图片落盘、`persisted_output_path`

**依赖**：无开放 T2 硬阻塞；与 **T2-P1-012** 共享 `tool_exec/` 热区，认领前同步 `develop`。

**协作接口**：`catalog.rs`、`tool_exec/`、`core/tools/web_fetch/*`、`core/permission/*`、`infra/config/types/tools.rs`、`infra/audit/mod.rs`。

**验收**：SSoT §10 矩阵全绿；`cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`、相关 `cargo test` 全绿。
