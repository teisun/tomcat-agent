### T2-P1-013 | `web_fetch` 工具

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `PENDING_INTEGRATION` |
| **负责人** | Jerry |
| **分支** | `feature/web-search`（当前开发分支） |
| **阻塞点** | — |
| **SSoT** | [`web_fetch.md`](../../../architecture/tools/web_fetch.md)（§2.3 决策表、§2.4 路线图、§4 协议、§10 测试）；冲突以该文档 + `src/` 为准 |
| **来源** | 002 迭代：补齐 URL 抓取；与 [`web_search.md`](../../../architecture/tools/web_search.md) 双工具分卡 |

**目标**：模型 `url` → 干净 Markdown（或二进制落盘返路径）；reqwest GET + **URL 校验 / SSRF 守卫** + 受控重定向；**不做**检索（归 T2-P1-012）。域名权限（PR-WF-D）后置。

**实现约束**（当前开发批次按 SSoT §2.3 / §2.4 执行，勿回退）：**不做**纯尾部截断；小页 inline、超大 markdown 落盘返 head + path + `total_chars`；429/5xx/超时走 warning 不抛 `Err`；本期安全边界先落 `validate.rs` 的 URL 校验 / SSRF 守卫，**不做**域名权限。

**子项**（路线图顺序固定；当前开发批次仅做 **PR-WF-A / PR-WF-S / PR-WF-B**，**PR-WF-D / PR-WF-P 本期不做**，见 SSoT §2.4）：

- [x] **PR-WF-A**：已注册 `web_fetch` 3 字段 schema，接入 `tool_exec/branches/web_fetch.rs`、runtime 注入链与缺 runtime 友好错误
- [x] **PR-WF-S**：已实现 HTTP + 重定向 + `validate.rs`（URL 校验 / SSRF 守卫）+ `html2md` + 正文体积分流 + `total_chars` + `ToolsWebFetchConfig` + moka 信封缓存；超阈值 `.md` 落盘
- [ ] **PR-WF-D**：`PermissionGate::Domain` + `check_domain`、三档域名配置、审计 `scope=domain`（**后置，本期不做**）
- [x] **PR-WF-B**：已实现 PDF/图等二进制分流 + magic 路由；与超大正文 `.md` 区分（`result=""` vs head 预览，见 SSoT §2.4.4）

**依赖**：无开放 T2 硬阻塞；与 **T2-P1-012** 共享 `tool_exec/` 热区，认领前同步 `develop`。

**协作接口**：`catalog.rs`、`tool_exec/`、`core/tools/web_fetch/*`、`core/permission/*`、`infra/config/types/tools.rs`、`infra/audit/mod.rs`。

**验收**：SSoT §10 当前批次通过；`cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test web_fetch --lib`、`cargo test --test web_fetch_tool_tests`、`cargo test --test tool_catalog_doc` 已通过；可选 `PI_LIVE_WEB_FETCH=1`（`E2E-WEB-FETCH-001`）保留。
