### T2-P1-015 | 会话模式与多会话并发

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `DONE` |
| **负责人** | Jerry |
| **分支** | `feature/optimize` |
| **阻塞点** | — |
| **SSoT** | [`session-modes.md`](../../../architecture/session-modes.md)（唯一真相；冲突以该文档 + `src/` 为准） |
| **当前口径** | 隐藏兼容别名 `chat -> code`；开发阶段旧 `sessions.json` 不做兼容迁移，`init` 与直接使用路径统一直接重建；`ChatContext` 三层迁移已完成，P3 仅保留 TUI / 前台 demux 等用户暴露能力 |

**Follow-up（`feature/optimize01`）**：2026-06-11 在 `feature/optimize01` 追加 D15「事件信封」收口，统一由 `ScopedEventEmitter` 把同一 `session_id` 写入 wire `payload.sessionId` 与 `EventContext.session_id`，并补齐 `event_bus` / `events` / `agent_loop` / `api/chat` 相关回归测试。该分支视为本卡在 `develop` 合入后的补漏 / 集成分支，不回滚本卡 `DONE` 状态；本轮验收与合并记录写入 `docs/status/feature-optimize01.md` 与 `docs/status/develop.md`。**闭环**：该 follow-up 已于 2026-06-11 本地合入 `develop`，并完成 develop 侧 `run-integration-tests.sh all` 复跑通过，未发现新增 develop-only 缺口。
