### T2-P1-012 | `web_search` 工具

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `TODO` |
| **负责人** | — |
| **分支** | `feature/web-search`（建议；认领时自定） |
| **阻塞点** | — |
| **SSoT** | [`web_search.md`](../../../architecture/tools/web_search.md)（§2.4 路线图、§4 协议、§10 测试）；冲突以该文档 + `src/` 为准 |
| **来源** | 002 迭代：补齐联网检索；与 [`web_fetch.md`](../../../architecture/tools/web_fetch.md) 双工具分卡 |

**目标**：模型 `query` → 归一化 `hits[]`；`backend=auto` 若项目 catalog 存在 `capabilities.web_search == true` 的 hosted 候选模型则优先 `openai(hosted)`，否则 Tavily→Brave→Serper；当前对话模型 wire 不参与 hosted 首选判断。

**子项**（顺序固定，细节见 SSoT §2.4）：

- [ ] **PR-WS-A**：catalog 6 字段 schema、`tool_exec/branches/web_search.rs` 占位、system_prompt
- [ ] **PR-WS-S**：`WebSearchBackend` + Tavily/Brave/Serper、`ToolsWebSearchConfig`、`moka` 缓存
- [ ] **PR-WS-O**：Responses `tools[]` 注入 `{type:web_search}`、`openai_server.rs` 块归一化、`capabilities.web_search` 能力位
- [ ] **PR-WS-W**：`normalize_hits` SSRF + domain 过滤

**依赖**：无开放 T2 硬阻塞；**PR-WS-O** 热区 `openai_responses/*`（T2-P1-010 已合入）。

**协作接口**：`catalog.rs`、`tool_exec/`、`core/tools/web_search/*`、`infra/config/types/tools.rs`、`openai_responses/{mod,payload}.rs`、`catalog.rs::Capabilities`。

**验收**：SSoT §10 矩阵全绿；`cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`、相关 `cargo test` 全绿。
