### T2-P1-012 | `web_search` 工具

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `DONE` |
| **负责人** | Jerry |
| **分支** | `feature/web-search` |
| **阻塞点** | — |
| **SSoT** | [`web_search.md`](../../../architecture/tools/web_search.md)（§2.4 路线图、§4 协议、§10 测试）；冲突以该文档 + `src/` 为准 |
| **来源** | 002 迭代：补齐联网检索；与 [`web_fetch.md`](../../../architecture/tools/web_fetch.md) 双工具分卡 |

**目标**：模型 `query` → 归一化 `hits[]`；`backend=auto` 若项目 catalog 存在 `capabilities.web_search == true` 的 hosted 候选模型则优先 `openai(hosted)`，否则 Tavily→Brave→Serper；当前对话模型 wire 不参与 hosted 首选判断。

**子项**（顺序固定，细节见 SSoT §2.4）：

- [x] **PR-WS-A**：catalog 6 字段 schema、`tool_exec/branches/web_search.rs` 占位、system_prompt
- [x] **PR-WS-S**：`WebSearchBackend` + Tavily/Brave/Serper、`ToolsWebSearchConfig`、`moka` 缓存
- [x] **PR-WS-O**：hosted sidecar `openai_server.rs` 请求 + 块归一化、`capabilities.web_search` 能力位（不走主对话 `openai_responses` 注入）
- [x] **PR-WS-W**：`normalize_hits` SSRF + domain 过滤

**依赖**：无开放 T2 硬阻塞；**PR-WS-O** 热区 `openai_responses/*`（T2-P1-010 已合入）。

**协作接口**：`catalog.rs`、`tool_exec/`、`core/tools/web_search/*`（含 `openai_server.rs` sidecar）、`infra/config/types/tools.rs`、`catalog.rs::Capabilities`。

**验收**：已在 develop 侧复跑转绿：`cargo build --release`、`cargo clippy --all-targets -- -D warnings`、`cargo test --lib`（1421 passed, 1 ignored）、`./scripts/run-integration-tests.sh integration`（含 `web_search_tool_tests` 5 passed、`cli_tests` 87 passed）与在本机配置 Tavily key 后执行 `cargo test --test web_search_tool_tests live_tavily_search_smoke -- --nocapture` 全绿。
