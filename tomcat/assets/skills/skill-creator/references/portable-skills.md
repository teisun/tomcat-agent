# Portable skills

A portable skill keeps its `SKILL.md` header small, UTF-8, and independent of one user’s machine. The required fields are a lowercase hyphen-case `name` and a one-line `description`. Keep reusable automation in `scripts/` and longer material in `references/`.
