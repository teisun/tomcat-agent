### T2-P0-011 | large-file-edit-strategy | 大文件编辑策略

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **状态** | `DONE`（**2026-05-08 核对**：以 **T2-P0-016 / T2-P0-017** 的契约与 primitive 实现为主交付；**未**实现初版方案 ② 的 `write` 超阈值 hint 配置项——现网路径被认为**更合理**：先 `read`、再 `edit`/`hashline_edit`，配合 `Stale` / `NoPriorRead` 与多段 `edits[]`，比「大字符串软提示」更可审计） |
| **负责人** | — |
| **分支** | 原 `feature/large-file-edit-strategy`；**实际**：随 `develop` 上 **strengthen-four-core-tools** / **edit 专项** 已落地 |
| **阻塞点** | — |
| **关联报告** | [compaction-prompt-cc-vs-pi.md §5.7 Anti-goals](../../../docs/reports/compaction-prompt-cc-vs-pi.md) 第 3 行 |

**目标（保留表述）**：让 Agent 更新大文件时**偏好**多次精确 `edit`（含 `edits[]` / `hashline_edit`）而非整文件 `write` 大 `content`，减轻单条 tool 上下文压力；**与 compaction 无关**。

**子项（2026-05-08 改判）**：
- [x] **调研**：不另起 plan 短文；结论以 **`docs/architecture/tools/edit.md`** / **`write.md`** + **`src/core/tools/contract/catalog.rs`** + **`src/core/llm/system_prompt.rs`** 为单一事实源（原卡片要求的路径易与架构文档重复维护，已废弃）。
- [x] **方案落地（组合，优于初版三选一）**：
  - **① System prompt**：`src/core/llm/system_prompt.rs` — `edit` 精确改 + `edits[]` 多段、**ORIGINAL snapshot**；`write` 仅新文件或**整文件重写**。
  - **③ Tool description**：`src/core/tools/contract/catalog.rs` — `write` 明示 *Prefer edit for small surgical changes to existing files*；`edit` / `hashline_edit` 长描述约束读戳与 Shape B。
  - **② 大字符串 hint / `executor.write_file_hint_threshold_chars`**：**刻意不做**；由 **read 戳 + edit 语义 + secrets/overwrite 门禁** 承担，避免「只 hint 不拦」的模糊地带。
- [ ] **集成测试「1MB 改 3 行」**：**未**专立 E2E；接受风险为「仅靠模型遵守契约」。若后续要硬验收，在 `cli_tests` 加场景即可（不阻塞本卡 DONE）。

**依赖**：T2-P0-002、T2-P0-005 — 已满足。

**被依赖**：—

**协作接口**：`PrimitiveExecutor`、`SystemPrompt`、`BuiltinToolCatalog`（无新增公共 trait）。

**验收（改判）**：
- [x] 大文件策略与 compaction 解耦，且在 **executor + prompt + catalog** 闭环。
- [x] 无新 panic；`write` 仍可用于合法全量重写。
- [ ] 初版要求的 **1MB E2E** — 见上，标为可选后续。

**说明**：初版路径名 `edit_file`/`write_file` 已统一为短名 **`edit`/`write`**（T2-P0-016 PR-A）；本卡正文保留历史用语处已在 TODOS `#T-043` 对齐。

---
