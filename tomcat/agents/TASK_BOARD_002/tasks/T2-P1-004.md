### T2-P1-004 | review-verification | reviewer 内联子 Agent + 多 Agent 基础设施

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `PENDING_INTEGRATION` |
| **负责人** | Tom |
| **分支** | `feature/plan-mode-enhance`（与 T2-P1-002/003 同分支交付） |
| **阻塞点** | — |
| **架构文档** | [`docs/architecture/tools/reviewer.md`](../../../docs/architecture/tools/reviewer.md)（**已定稿**）；派发与 OOD：[`multi-agent.md` §14](../../../docs/architecture/multi-agent.md)；挂接：[`create-plan.md` CP-D](../../../docs/architecture/tools/create-plan.md#424-cp-d内联-reviewer见-reviewermd)、[`plan-runtime.md` PR-PLC](../../../docs/architecture/plan-runtime.md#423-pr-plcreviewer-internal-subagent-dispatch-与-plan-build) |

**目标**：`create_plan` 落盘成功后 **同步** `PlanRuntime::dispatch_reviewer` → `AgentRegistry::spawn_subagent_internal`（**不进** LLM catalog）；输出 **`ReviewSummary` 顾问摘要**（**无** verdict gate）；落 `transcript.plan.review` + 回填 `ToolResult.review`；**不**改 `PlanFile.mode`；进 EXEC **仅**由用户 `/plan build`（**T2-P1-002**）。并落地 reviewer 所需的 **multi-agent Phase 2/3 最小子集**（`AgentRegistry`、`spawn_subagent_internal`、`CascadeAbort`、`SubAgentStart`/`End`）。

**已定稿选型（§4.1 摘要）**

| 维度 | 拍板结论 | 方案 § |
|------|----------|--------|
| **R-DispatchEntry** | internal subagent dispatch，**非** LLM 工具 | [reviewer §4.1](../../../docs/architecture/tools/reviewer.md#41-落地选型决策表) |
| **RV2** | 同步 await；摘要同条 `create_plan` ToolResult | §4.1 RV2 |
| **RV3** | `allowed_tools` 硬编码：默认 `{read,grep,find,todos}`；`allow_review_edit` → `+update_plan,edit`；**永不含** `create_plan` | §4.1 RV3、[§5.2](../../../docs/architecture/tools/reviewer.md#52-allowed_tools) |
| **RV4** | 无 verdict gate；**不**挡 `/plan build` | §4.1 RV4 |
| **RV10–RV15** | 双注册表 OOD；RV14 先释放 plan lock 再 spawn；RV15 reviewer 改 plan 后父 `reload_from_disk` | [§4.5](../../../docs/architecture/tools/reviewer.md#45-ood-与双注册表reviewer-的嵌套但不开新-chatcontext) |
| **MA（multi-agent）** | Phase 2：`AgentRegistry` + `AgentLoopConfig` 扩展；Phase 3 **子集**：`spawn_subagent_internal` + 子 Agent 事件（**本期可不交付** LLM `dispatch_agent` 工具） | [multi-agent §14.8](../../../docs/architecture/multi-agent.md#148-mvp-降级与实施顺序) |

**子项（按方案拆分；勾选即闭环）**

**A. 多 Agent 基础设施（reviewer 前置）**

- [ ] **MA-2a**：`src/core/agent_registry.rs` — `AgentRegistry` / `AgentHandle`；`register` / `unregister`；`MAX_CONCURRENT_AGENTS` / `MAX_CHILDREN_PER_AGENT` — [§14.3.2](../../../docs/architecture/multi-agent.md#1432-agentregistry进程级)
- [ ] **MA-2b**：`AgentLoopConfig` 新增 `parent_session_id`、`spawn_depth`、`subagent_type`（`SubagentType::Reviewer` 内部枚举，**不出现在** `dispatch_agent` schema）— [§14.3.3](../../../docs/architecture/multi-agent.md#1433-agentloopconfig-扩展)
- [ ] **MA-3a**：`AgentRegistry::spawn_subagent_internal` — 唯一子 `AgentLoop::new` 点之一；栈上拥有子 loop — [§14.4.2.1–2.2](../../../docs/architecture/multi-agent.md#14421-调用栈与代码落点)
- [ ] **MA-3b**：`CascadeAbort`；`infra/events.rs` 新增 `SubAgentStart` / `SubAgentEnd` — [§14.4.5](../../../docs/architecture/multi-agent.md#1445-cascadeabort级联中止)、[§14.5](../../../docs/architecture/multi-agent.md#145-事件系统扩展)

**B. reviewer（[`reviewer.md` §4.2](../../../docs/architecture/tools/reviewer.md#42-实施点拟定)）**

- [ ] **RV-A**：`PlanRuntime::dispatch_reviewer(plan_id, allow_review_edit)`（**非** free function）；`write_plan` **释放 lock 后**再 spawn（RV14）；`AgentRegistry::spawn_subagent_internal` 同步 await — [§4.2.1](../../../docs/architecture/tools/reviewer.md#421-rv-adispatch_reviewer-与-internal-dispatch)、[§4.3 API](../../../docs/architecture/tools/reviewer.md#43-派发入口api-形态)
- [ ] **RV-B**：`REVIEWER_SYSTEM_PROMPT` + `allowed_tools` 硬编码分支；`resolve_internal_tools` 与父 catalog 交集 — [§4.2.2](../../../docs/architecture/tools/reviewer.md#422-rv-ballowed_tools-与-system-prompt)、[§5.1–5.2](../../../docs/architecture/tools/reviewer.md#51-reviewer-system-prompt常量内容)
- [ ] **RV-C**：解析 `<review>` block → `ReviewSummary`；写 `transcript.plan.review` + `ToolResult.review`；**不写** frontmatter；解析失败 → `aborted: true` — [§4.2.3](../../../docs/architecture/tools/reviewer.md#423-rv-creviewsummary-解析与摘要落点)
- [ ] **RV-D**：`subagent_type==Reviewer` 时 `create_plan` **跳过**二次 reviewer；`tool_exec.edit` 路径白名单 + `## Review` 段 diff guard；`update_plan` 继承既有门控 — [§4.2.4](../../../docs/architecture/tools/reviewer.md#424-rv-d防递归内联-reviewer--改稿落点守卫)
- [ ] **RV-E**：`spawn_depth = parent+1`；`max_review_rounds` advisory warning；父 abort → 子 abort；reviewer abort 不挡 `create_plan` 成功 — [§4.2.5](../../../docs/architecture/tools/reviewer.md#425-rv-eabort轮次与深度)、[§8.3](../../../docs/architecture/tools/reviewer.md#83-abort-语义)

**C. `create_plan` 联调（[`create-plan.md` CP-D](../../../docs/architecture/tools/create-plan.md#424-cp-d内联-reviewer见-reviewermd)）**

- [ ] **CP-D**：`tool_exec::create_plan` 串行：`write_plan` → `dispatch_reviewer` → `plan.create` + `plan.review` transcript → `ToolResult { plan_id, path, review }`；`allow_review_edit` **仅** runtime 配置（`TOMCAT_REVIEWER_DEFAULT_ALLOW_EDIT`，默认 false）

**非目标（本卡不做）**

| 项 | 说明 |
|----|------|
| LLM 可见 `reviewer` / `review_self` / `review_by_child` 工具 | §3.1；走 internal dispatch |
| 红绿灯 verdict gate EXEC / PLAN 重规划 | 已被方案取代 |
| reviewer 写用户仓库代码 | 无 `bash`/`write`；`edit` 仅 plan `## Review` |
| reviewer 决定 `PlanFile.mode` | `/plan build` + runtime 派生（**T2-P1-002**） |
| LLM `dispatch_agent` 工具全量（Phase 3 剩余） | 可后续迭代；本期 reviewer 不依赖 |
| `ChatContextRegistry` 多会话并发（Phase 2 产品面） | 与 reviewer 正交；registry 壳可在 **T2-P1-002** 先 per-session |

**依赖**：**T2-P1-002**（`PlanRuntime`、`create_plan`、`file_store`、catalog）、**T2-P1-003**（E2E 联测可选）

**被依赖**：**T2-P1-005**（Feedback 等后续）

**协作接口**：
- 消费：**T2-P1-002** `PlanRuntime` / `file_store` advisory lock、**T2-P1-002** `update_plan`（reviewer 改稿）、`ToolRegistry`（内部 catalog 子集）
- 提供：`PlanRuntime::dispatch_reviewer`、`ReviewSummary`、`AgentRegistry::spawn_subagent_internal`（reviewer 与未来 `dispatch_agent` 共用）

**验收标准**（[`reviewer.md` §3 G1–G8](../../../docs/architecture/tools/reviewer.md#3-目标与设计原则)、[§11](../../../docs/architecture/tools/reviewer.md#11-测试矩阵验收)；[`plan-runtime.md` §11](../../../docs/architecture/plan-runtime.md#11-测试矩阵验收) reviewer 相关行）

| 指标 | 可观察结果 |
|------|------------|
| G1 | 任何 catalog / schema **无** reviewer |
| G2 | `spawn_subagent_internal` + `SubagentType::Reviewer` |
| G3 | 默认工具集无 `create_plan`/`bash`/`checkpoint` |
| G4 | `/plan build` **不读** `ReviewSummary` |
| G5–G6 | `allow_review_edit` 时 `update_plan` + scoped `edit`；`max_review_rounds` 仅 warning |
| G7–G8 | 父 abort 级联；摘要仅 transcript + tool result |

- [ ] MA-2a/b + MA-3a/b + RV-A–RV-E + CP-D 子项全部勾选
- [ ] 单元 / 集成：`reviewer_not_in_catalog`、`reviewer_uses_internal_dispatch_via_agent_registry`、`reviewer_default_allowed_tools_*`、`reviewer_summary_does_not_gate_exec`、`build_command_ignores_review_summary`、`reviewer_summary_lands_in_transcript_plan_review`、`reviewer_edit_scoped_to_review_section`、`reviewer_create_plan_does_not_redispatch_reviewer`、`reviewer_aborts_on_parent_cascade`、`create_plan_internally_dispatches_reviewer`（PENDING → ✅）
- [ ] E2E：与 **T2-P1-002** `E2E-PLAN-001` 联跑 — `create_plan` 后同条 tool result 含 `review` 摘要；用户 `/plan build` 进 EXEC（与 review 内容无关）
- [ ] 门禁：integration / all `EXIT_CODE=0`

**已被方案取代的旧口径**：`review_self` / `review_by_child` LLM 工具、**红绿灯**（全绿/黄/红）gate、reviewer 决定里程碑完成、独立 review 子流程与 PLAN 面板强绑定 — 见 [reviewer.md §13](../../../docs/architecture/tools/reviewer.md#13-历史决策)、[plan-runtime.md §13](../../../docs/architecture/plan-runtime.md#13-历史决策--跨文档修订)。

---
