### T2-P1-002 | plan-mode-enhance | PLAN 模式增强（PlanRuntime）

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `DOING` |
| **负责人** | Tom |
| **分支** | `feature/plan-mode-enhance` |
| **阻塞点** | E2E-PLAN-001～016 CLI 子进程 + mock HTTP 套件转 PR-PLG 独立交付（chat_loop 主路径已接通；plan_runtime 子树 114 单测 + 8 集成 + D10 catalog 一致性全绿） |
| **架构文档** | [`docs/architecture/plan-runtime.md`](../../../docs/architecture/plan-runtime.md)（**已定稿**）；工具契约：[`create-plan.md`](../../../docs/architecture/tools/create-plan.md)、[`update-plan.md`](../../../docs/architecture/tools/update-plan.md)、[`todos.md`](../../../docs/architecture/tools/todos.md)、[`planner.md`](../../../docs/architecture/tools/planner.md)；**EXEC 完工代码验证（verifier）**：[`plan-exec-code-verification.md`](../../../docs/architecture/plan-exec-code-verification.md)（**已定稿**，与 reviewer 分拆） |
| **上位调研** | [`docs/reports/plan-mode-and-checkpoint-survey.md`](../../../docs/reports/plan-mode-and-checkpoint-survey.md)；verifier 横向调研索引：[`plan-todo-post-completion-verification-survey.md`](../../../docs/architecture/plan-todo-post-completion-verification-survey.md) |

**目标**：落地 **`PlanRuntime` / `TodoRuntime` per-session OOD** 与 **`/plan` 本地命令族**闭环：`/plan` → PLAN 模式（`create_plan` + `ask_question` 见 **T2-P1-003**）→ 用户 `/plan build` 进 EXEC → `update_plan` / `todos` 推进 → 里程碑 checkpoint（消费 **T2-P1-001** `CheckpointStore`）→ 全 completed 自动收口；**reviewer 内联派发**见 **T2-P1-004**；**EXEC 完工 verifier**（`all_completed` 后 internal 子 Agent 验货，默认不 gate）见本卡 **PR-V0～V3** 与 [`plan-exec-code-verification.md`](../../../docs/architecture/plan-exec-code-verification.md)。

**已定稿选型（§4.1 决策表摘要）**

| 维度 | 拍板结论 | 方案 § |
|------|----------|--------|
| **R1** 命令入口 | `/plan` / `/plan exit` / `/plan build <plan_id/path>` 全部为 chat 本地 slash，**不进** tool catalog | [§4.1 R1](../../../docs/architecture/plan-runtime.md#41-落地选型决策表精简版) |
| **R2** Todos 模型 | D 方案：`todos` 写 `TodoFile` + `update_plan` 写 `PlanFile.frontmatter`；共享 `apply_todos_op` | §4.1 R2、[update-plan.md §2](../../../docs/architecture/tools/update-plan.md#2-与-todos--create_plan-的关系) |
| **R3** 进执行态 | **仅** `/plan build`；reviewer **不**自动 build | §4.1 R3 |
| **R4** Review | reviewer **仅辅助**、**非 gate**；摘要落 transcript（**T2-P1-004** 实现派发） | §4.1 R4 |
| **R5** 持久化 | `~/.tomcat/plans/<slug>_<hash>.plan.md` + advisory lock；frontmatter 四方协同写 | §4.1 R5、[create-plan.md §5](../../../docs/architecture/tools/create-plan.md#5-planfile-文件协议) |
| **R6** Catalog | PLAN：`create_plan` + `ask_question` + `todos` + `update_plan` + 写盘白名单；EXEC/CHAT：`todos` + `update_plan` − `create_plan` | §4.1 R6 |
| **R7** Checkpoint | 单向 `PlanRuntime → CheckpointStore` | §4.1 R7 |
| **R8** 面板 | `TodosPanel` 复用 `BashTaskRegistry`，不新建进程管理器 | §4.1 R8 |
| **R9** 里程碑 | `Milestone[]` + 完成派生 `mode=completed` | §4.1 R9 |
| **R10** OOD | `TodoRuntime` / `PlanRuntime` per-session 挂 `ChatContext` | §4.1 R10、[§6](../../../docs/architecture/plan-runtime.md#6-todoruntime--planruntime-per-session-ood-设计) |
| **R11** user prefix | PLAN/EXEC 注入 `[mode: PLAN]` / `[mode: EXEC plan_id=...]` | §4.1 R11 |
| **V1–V10** verifier（EXEC 完工） | 与 reviewer 分拆；`all_completed` → `dispatch_verifier`（internal）；**仅** `[plan].verify_gate` 一项配置（枚举 `soft\|gate`，默认 `soft`），**frontmatter 零改造**；`VERIFIER_MAX_TURNS=64` 代码常量；`VerifySummary` 双通道回传（transcript `plan.verify` + `update_plan` tool result.verify） | [verifier §4.1](../../../docs/architecture/plan-exec-code-verification.md#41-落地选型决策表) |

**子项（按方案 §4.2 PR 拆分；勾选即闭环该 PR）**

- [ ] **PR-PLA**：`/plan` 命令面 + `PlanMode` / `PlanRuntime` 骨架；`parse.rs` 本地消费；PLAN/EXEC `<system_reminder>` + user message 模式前缀；`visible_tools_for_mode` 动态 catalog；`PlanRuntime::recover` 扫描 `~/.tomcat/plans/` — 见 [§4.2.1](../../../docs/architecture/plan-runtime.md#421-pr-pla命令面与-planexec-模式骨架)
- [ ] **PR-PLB（本卡范围）**：built-in `todos` + `update_plan` + `create_plan`（**不含**内联 reviewer，见 **T2-P1-004** CP-D）；`TodoRuntime` / `PlanRuntime`；`file_store` advisory lock + atomic write + frontmatter round-trip；`apply_todos_op` 单写入路径 — 见 [§4.2.2](../../../docs/architecture/plan-runtime.md#422-pr-plbtodos--createplan--askquestion-工具与计划文件)、[create-plan CP-A/B/C/E/F/G](../../../docs/architecture/tools/create-plan.md#42-实施点拟定)、[update-plan §4–§8](../../../docs/architecture/tools/update-plan.md#4-门控矩阵mode--op--planmode)
- [ ] **PR-PLC（本卡范围）**：`/plan build` 闸门（`无 active plan && 无 active todos` + `PlanFile.mode ∈ {planning,pending}`）+ build 时 5 件事（session 绑定、mode=executing、reminder swap、user prefix + 首轮 plan 全文 meta、catalog swap）；可选 `CheckpointStore::record(Manual{plan_build:…})` — 见 [§4.2.3](../../../docs/architecture/plan-runtime.md#423-pr-plcreviewer-internal-subagent-dispatch-与-plan-build)（reviewer 派发段落归 **T2-P1-004**）
- [ ] **PR-PLD**：`TodosPanel` 三段渲染 + `BashTaskRegistry` 摘要投影；todo↔bash 弱绑定；`RefreshNotifier` 200ms 节流；transcript `plan.todos` / `plan.panel` — 见 [§4.2.4](../../../docs/architecture/plan-runtime.md#424-pr-pld-todospanel-与-bash-输出投影)、[todos.md §7](../../../docs/architecture/tools/todos.md)
- [ ] **PR-PLE**：`Milestone[]`；milestone 完成 → `record(Milestone{…})`（`[plan] auto_checkpoint_on_milestone`）；全 todo completed → `mode=completed` + reminder/catalog/prefix 复位 — 见 [§4.2.5](../../../docs/architecture/plan-runtime.md#425-pr-ple里程碑拆分与-checkpoint-hook--mode-自动派生)
- [ ] **PR-PLF**：PLAN 期 `write/edit` 路径白名单 `~/.tomcat/plans/*.plan.md`；frontmatter diff 硬拒；EXEC `cancel_token` → `mode=pending` + `/plan build` 续跑 — 见 [§4.2.6](../../../docs/architecture/plan-runtime.md#426-pr-plf-raw-writeedit-拦截--cancel_token-续跑)、[create-plan CP-G](../../../docs/architecture/tools/create-plan.md#427-cp-gplan-模式-raw-writeedit-拦截)

**子项 — EXEC 完工代码验证（verifier；方案 [`plan-exec-code-verification.md`](../../../docs/architecture/plan-exec-code-verification.md) §4.2；依赖 **T2-P1-004** 已落地的 `spawn_subagent_internal`）**

- [ ] **PR-V0**：`executor.txt` / `planner.txt` 追加 Mini 验证 + P0–P6 命令发现（§4.1.1 合入用 Prompt）；**不** gate `update_plan(completed)` — 见 [§4.2 PR-V0](../../../docs/architecture/plan-exec-code-verification.md#42-实施点分期)
- [ ] **PR-V1**：`SubagentType::Verifier`、`VERIFY_SYSTEM_PROMPT`、`VerifySummary` 解析、`PlanRuntime::dispatch_verifier`；`VERIFIER_MAX_TURNS = 64`（代码常量，不进 TOML）；`update_plan` 在 `all_completed` 与 `set_mode_completed` 之间同步 await — 见 [§4.2.1 调度要点](../../../docs/architecture/plan-exec-code-verification.md#421-pr-v1-调度要点ascii)
- [ ] **PR-V2**：`transcript.plan.verify` 事件落盘；`update_plan` tool result JSON 增加 `verify: VerifySummary \| null` 字段（双通道回传主 Agent） — 见 [§5.2](../../../docs/architecture/plan-exec-code-verification.md#52-verifysummary单一事实源拟定-plan_runtimeverifyrs)
- [ ] **PR-V3**：`infra/config` 增 `[plan].verify_gate: String`（枚举 `soft\|gate`，默认 `"soft"`）；`verify_gate="gate" && verdict="fail"` 时 runtime 跳过 `set_mode_completed`、保持 `mode=Executing`、tool result.verify 仍回传 — 见 [§9 配置](../../../docs/architecture/plan-exec-code-verification.md#9-配置与环境变量)

**非目标（本卡不做）**

| 项 | 推给 | 说人话 |
|----|------|--------|
| 重做 `CheckpointStore` / `/restore` | **T2-P1-001**（已定稿） | 快照底座不重写。 |
| `ask_question` 工具与 UI panel | **T2-P1-003** | 结构化提问独立卡。 |
| reviewer `dispatch_reviewer` + `AgentRegistry::spawn_subagent_internal` | **T2-P1-004** + [multi-agent.md §14](../../../docs/architecture/multi-agent.md) Phase 2/3 | 内联审稿员另卡。 |
| LLM 可见 `dispatch_agent` 工具（Phase 3 全量） | 后续迭代 / 与 reviewer 解耦 | 本期 reviewer 只需 internal dispatch。 |
| verifier 暴露为 LLM catalog 工具 | [`plan-exec-code-verification.md` §3.2](../../../docs/architecture/plan-exec-code-verification.md#32-非目标) | 与 reviewer 同：internal dispatch only。 |
| 替代 CI / `pi_agent_rust` `./verify` | CI / 用户脚本 | 不在 agent 里重造流水线。 |
| `/plan show`、`/goal`、reviewer verdict gate、自动 build | 方案 §3.2 / §13 | 不进本期验收。 |
| `agents/plan/<timestamp>.md` 进展文件、红绿灯 gate | 已被方案取代（见下） | 别按旧任务卡做。 |

**依赖**：**T2-P1-001**（`CheckpointStore` + Manual/Milestone API）；**PR-V1+** 依赖 **T2-P1-004**（`AgentRegistry::spawn_subagent_internal`，本分支已落地）

**被依赖**：**T2-P1-003**（`ask_question` 挂 PLAN catalog）、**T2-P1-004**（`create_plan` 完成后调 reviewer）

**协作接口**：
- 消费：`Arc<dyn CheckpointStore>`（**T2-P1-001**）、`BashTaskRegistry`、`ToolRegistry` / `tool_exec`、`session` transcript
- 提供：`PlanRuntime` / `TodoRuntime`（per-session）、`/plan` 命令处理、`PlanFile` 读写与 catalog 过滤；**PR-V2+** 提供 `/plan verify`、transcript `plan.verify`

**验收标准**（与 [`plan-runtime.md` §3.1 G1–G10](../../../docs/architecture/plan-runtime.md#31-观察指标表)、[§11 测试矩阵](../../../docs/architecture/plan-runtime.md#11-测试矩阵验收) 对齐；reviewer / ask 相关用例在对应任务卡勾选）

| 指标 | 可观察结果 |
|------|------------|
| G1 | `/plan` 三命令本地解析，不进 LLM 普通 user 文本 |
| G2 | `todos` 返回完整 items snapshot；单 `in_progress` 约束 |
| G3 | 仅 `/plan build` 进 EXEC；build gate 生效 |
| G5 | `~/.tomcat/plans/*.plan.md` round-trip；advisory lock 冲突可见错误 |
| G6 | milestone 完成可 `record(Milestone{…})`（warning-only 失败） |
| G7 | TodosPanel 反映 bash 任务状态 |
| G8 | PLAN catalog 收紧；raw frontmatter 写被拒 |
| G9–G10 | per-session Runtime；PLAN/EXEC user message 前缀 |
| G4 | reviewer 摘要落 transcript、不写 frontmatter、不 gate build（**与 T2-P1-004 联测**） |
| **VG1–VG6**（verifier） | 与 [`plan-exec-code-verification.md` §3.1 G1–G6](../../../docs/architecture/plan-exec-code-verification.md#31-观察指标表) 对齐：reviewer≠verifier；checks 含 command；默认不 gate；可选 `verify_gate`；工具白名单；bash 仍走 permission |

- [ ] PR-PLA–PLF（本卡 PlanRuntime 范围）子项全部勾选；**T2-P1-003** `ask_question`、**T2-P1-004** reviewer 各自任务卡勾选
- [ ] **PR-V0～V3**（verifier）子项全部勾选 — 单测/集成见 verifier [§11 T-V1～T-V7](../../../docs/architecture/plan-exec-code-verification.md#11-测试矩阵验收)（`verifier_spawned_on_all_completed`、`verifier_blocked_write_tools`、`verify_gate_blocks_completed_on_fail` 等 PENDING → ✅）
- [ ] 单元 / 集成：§11 中 `parse_plan_commands`、`plan_enter_injects_planner_reminder_into_system`、`todos_returns_full_items_snapshot`、`update_plan_visible_in_all_modes`、`plan_build_requires_no_active_plan_or_todos`、`plan_file_round_trip_frontmatter`、`cancel_token_demotes_executing_to_pending`、`milestone_completion_can_record_checkpoint` 等（PENDING → ✅）
- [ ] E2E：`E2E-PLAN-001`（3 里程碑 + `/plan build` 进 EXEC）；`E2E-PLAN-002`（EXEC Ctrl+C → pending → 再 build 续跑）
- [ ] 门禁：按 [`INTEGRATION_MERGE_AND_ACCEPTANCE.md`](../../../agents/INTEGRATION_MERGE_AND_ACCEPTANCE.md) 跑 `scripts/run-integration-tests.sh integration` / `all`，日志 `EXIT_CODE=0`

**已被方案取代的旧口径**（勿按任务卡旧版实现）：PLAN 执行面板自带 bash 子进程、Planner/review **两个** LLM 子 Agent 工具、`agents/plan/<timestamp>.md` 锁与进展文件、reviewer **红绿灯** gate EXEC、`/plan apply`、显式 `/plan close`、`ask_user` 通用提问工具 — 见 [plan-runtime.md §13](../../../docs/architecture/plan-runtime.md#13-历史决策--跨文档修订)。

---
