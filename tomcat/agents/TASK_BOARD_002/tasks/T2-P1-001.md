### T2-P1-001 | checkpoint-resume | Checkpoint + 断点续跑

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `DONE`（已合并 `feature/checkpoint-resume` → `develop`；§4 全量门禁本地 `EXIT_CODE=0`：lib 890/0、parallel 全绿、checkpoint_cli_e2e 5/0、cli_tests 78/0、wasmedge_e2e_tests 39/0） |
| **负责人** | Jerry |
| **分支** | `feature/checkpoint-resume` |
| **阻塞点** | 无功能阻塞；全仓 `cargo fmt --check` 仍有无关既存漂移，但不构成本卡门禁阻塞 |
| **架构文档** | [`docs/architecture/tools/checkpoint-resume.md`](../../../docs/architecture/tools/checkpoint-resume.md)（**已定稿**；实现以仓库代码为准，契约以该文 §4.1 / §4.2 / §5 / §11 为准） |
| **上位调研** | [`docs/reports/plan-mode-and-checkpoint-survey.md`](../../../docs/reports/plan-mode-and-checkpoint-survey.md) |

**目标**：在 **TurnEnd / Interrupt** 自动对工作区拍影子 Git 快照；用户通过 **`tomcat chat` 内 `/ckpt`、`/restore`** 按整树或路径还原磁盘；重启 **`tomcat chat` 恒 `Continue` hydrate**（跳过 `superseded`），**不**在启动自动改盘或推销回滚。对外提供稳定 **`CheckpointStore`** 供 T2-P1-002 PlanRuntime 消费。

**已定稿选型（§4.1 决策表摘要）**

| 维度 | 拍板结论 | 方案 § |
|------|----------|--------|
| **C1** 快照粒度 | 整工作区影子 Git（`GIT_DIR` 在 `~/.tomcat/...`，`GIT_WORK_TREE` = `agent_workspace_dir`）；无 git ⇒ `NoopStore` | [§4.1 C1](../../../docs/architecture/tools/checkpoint-resume.md#41-落地选型决策表维度取舍) |
| **C2** 写入时机 | **仅 TurnEnd + Interrupt**（+ Manual / Milestone API）；**无 ToolPre / ToolPost / Compaction kind** | §4.1 C2 |
| **C3** dedup | `(session_id, turn_id, kind)` 整轮去重，每 turn 每 kind ≤ 1 commit | §4.1 C3 |
| **C4** LLM 可见性 | **不进** tool catalog；模型不可调 ckpt | §4.1 C4 |
| **C5** transcript 耦合 | `message_anchor` 在元数据；**`superseded` + `Custom{checkpoint.restore}` 仅 TurnEnd/Interrupt restore**；`last_checkpoint_id` 同口径；启动恒 `Continue` | §4.1 C5、[§5.4](../../../docs/architecture/tools/checkpoint-resume.md#54-restore-语义transcript-与按路径还原) |
| **C6** restore 面 | **`/restore <ck> [--path …] [--dry-run]`** + `/ckpt list|show|diff`；**无** `tomcat session …` ckpt/restore；**无** `/ckpt prune` | §4.1 C6 |
| **C7** GC | **仅** `tomcat chat` 启动期默认后台 `prune(retention)`；`retention_max` / `retention_days` | §4.1 C7、[§9](../../../docs/architecture/tools/checkpoint-resume.md#9-配置与环境变量极简) |
| **C9** 无 git | `NoopStore` + warn；可选 **`[preflight] auto_install_git`** 后台装 git（PR-CKF 拟定） | §4.1 C9、[§2.2.2](../../../docs/architecture/tools/checkpoint-resume.md#222-chat-入口-git-预检与后台安装拟定) |
| **C10** 续跑 | `readline` 前 hydrate；`compute_resume_plan` **恒 `Continue`**（无 `Fresh` / `RewindTo`） | §4.1 C10、[§4.2.4](../../../docs/architecture/tools/checkpoint-resume.md#424-pr-ckd断点续跑transcript-hydrate启动恒-continue) |
| **C11** 预览 | `/restore --dry-run` + `/ckpt diff` | §4.1 C11 |
| **C12** 终端副作用 | restore **仅**文件树；DB / `pnpm install` 等 **非目标** | §4.1 C12、[§3.2](../../../docs/architecture/tools/checkpoint-resume.md#32-非目标) |
| **C13** 与用户 Git | 影子仓与用户 `.git` **物理分离** | §4.1 C13 |
| **C14** pre-rollback | **仅 TurnEnd** restore 前整树 `Manual` ckpt；失败 **fatal** | §4.1 C14、[§10](../../../docs/architecture/tools/checkpoint-resume.md#10-错误模型--截断--警告) |

**子项（按方案 §4.2 PR 拆分；勾选即闭环该 PR）**

- [x] **PR-CKA**：`CheckpointStore` trait + `ShadowGitStore` / `NoopStore`；`CheckpointId` / `CheckpointKind` / `CheckpointMeta`；注入 `AgentLoopConfig`；配置仅 `[checkpoint] retention_max` / `retention_days` — 见 [§4.2.1](../../../docs/architecture/tools/checkpoint-resume.md#421-pr-ckacheckpointstore--影子-git-后端)
- [x] **PR-CKB**：**TurnEnd**（`Completed` 且持久化后）+ **Interrupt**（partial 落盘且 flush 后）；dedup；**无 ToolPre** — 见 [§4.2.2](../../../docs/architecture/tools/checkpoint-resume.md#422-pr-ckb写入时机与钩子turnend--interrupt)
- [x] **PR-CKC**：`/ckpt list|show|diff` + **`/restore [--path …] [--dry-run]`**；TurnEnd pre-rollback；条件 `superseded` + `session.restore` 审计 — 见 [§4.2.3](../../../docs/architecture/tools/checkpoint-resume.md#423-pr-ckcrestore-命令面仅-chat-斜杠)
- [x] **PR-CKD**：`compute_resume_plan` 恒 `Continue`；启动期后台 `prune`；hydrate 跳过 `superseded`；`SessionEntry.last_checkpoint_id` — 见 [§4.2.4](../../../docs/architecture/tools/checkpoint-resume.md#424-pr-ckd断点续跑transcript-hydrate启动恒-continue)
- [x] **PR-CKE**：交叉引用 [`tool-catalog.md`](../../../docs/tool-catalog.md)、[`work-dir-and-data-layout.md`](../../../docs/architecture/work-dir-and-data-layout.md)、[`audit-log.md`](../../../docs/architecture/audit-log.md)、[`interrupt-and-cancellation.md §14.1`](../../../docs/architecture/interrupt-and-cancellation.md)；`scripts/test-groups.sh` 登记 `checkpoint` 分组 — 见 [§4.2.5](../../../docs/architecture/tools/checkpoint-resume.md#425-pr-cke文档与门禁)
- [x] **PR-CKF（拟定）**：`start_git_preflight` 与 rg 预检同构；`WIRE_GIT_PREFLIGHT`；`NoopStore → ShadowGitStore` 惰性切换 — 见 [§4.2.6](../../../docs/architecture/tools/checkpoint-resume.md#426-pr-ckfgit-预检与后台安装拟定)

**非目标（本卡不做）**：跨 session ckpt、PLAN 面板 / `agents/plan/*.md` 锁（**T2-P1-002**）、TUI 时间线 UI（**T2-P0-008**）、`tomcat session rollback`、启动期自动 `git checkout` 对齐盘态、`ResumePlan::Fresh` / `RewindTo` — 详见方案 [§3.2](../../../docs/architecture/tools/checkpoint-resume.md#32-非目标)。

**依赖**：**T2-P0-007**（中断 partial 落盘 + transcript 完整性；T-007 完整版 ckpt 能力在本卡收口）

**被依赖**：**T2-P1-002**（PlanRuntime 消费 `CheckpointStore` + Manual / Milestone API）

**协作接口**：
- 消费：`session`（transcript append / hydrate）、[`interrupt-and-cancellation.md`](../../../docs/architecture/interrupt-and-cancellation.md) Soft Interrupt 时序、`audit`（`session.restore` hostcall）
- 提供：`Arc<dyn CheckpointStore>`（`record / list / show / diff / restore / prune`）

**验收标准**（与方案 [§3.1 G1–G9](../../../docs/architecture/tools/checkpoint-resume.md#31-观察指标表与-11-验收一一对应)、[§11 测试矩阵](../../../docs/architecture/tools/checkpoint-resume.md#11-测试矩阵验收) 对齐）

| 指标 | 可观察结果 |
|------|------------|
| G1–G2 | TurnEnd / Interrupt 各写一张 ckpt（dedup 生效） |
| G3 | `/restore` 整树或 `--path` checkout；路径防穿越 |
| G4 | TurnEnd/Interrupt restore ⇒ `superseded` + `Custom{checkpoint.restore}`；Manual（含 pre-rollback）⇒ 仅改盘 |
| G5–G6 | 重启 hydrate 跳过 `superseded`；`compute_resume_plan` 恒 `Continue` |
| G7 | 启动后台 `prune` 不阻塞 readline；无 `/ckpt prune` |
| G8 | 无 git ⇒ `NoopStore`，transcript 续跑仍可用 |
| G9 | 运行中关终端 ≈ interrupt + Interrupt ckpt |

- [x] PR-CKA–CKF 子项全部勾选；PR-CKE 文档与 `checkpoint` 测试分组登记完成
- [x] 单测 / 集成：`turn_end_writes_checkpoint`、`interrupt_writes_checkpoint_after_partial_persist`、`checkpoint_integration_tests`、`chat_git_preflight_tests`、`startup_prune_scheduled_without_blocking_readline`、`init_context_state_skips_superseded_messages`、`resume_plan_always_continue` 已全部通过
- [x] E2E：`E2E-CLI-070` 中断后重启续聊；`E2E-CLI-075` `/restore` 从坏编辑恢复；`E2E-CLI-076` 运行中挂断后保留 partial transcript 与 `Interrupt` checkpoint
- [x] 门禁：按 [`INTEGRATION_MERGE_AND_ACCEPTANCE.md`](../../../agents/INTEGRATION_MERGE_AND_ACCEPTANCE.md) 使用 `scripts/run-integration-tests.sh integration` / `all` 后台日志模板执行，`.integration_test_output.log` 末尾均为 `EXIT_CODE=0`
- [x] **明确不做**：`tomcat session rollback`、启动自动对齐工作区、全量 `/restore` 一律 `superseded`

**已被方案取代的旧口径**（勿按任务卡旧版实现）：`agents/plan/` design.md、tool 前后 / compaction 钩子、`tomcat session rollback <id>`、按 checkpoint 自动 RewindTo — 见方案 [§13](../../../docs/architecture/tools/checkpoint-resume.md#13-历史决策已被本方案取代)。

---
