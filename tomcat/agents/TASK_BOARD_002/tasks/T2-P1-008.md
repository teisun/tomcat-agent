### T2-P1-008 | search-files-tool | 内置 search_files 只读搜索工具

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **状态** | `DONE`（**2026-05-08 核对**：`develop` 上已含 `search_files` 全链路；原卡 `PENDING_INTEGRATION` 与验收④草稿已过时） |
| **负责人** | Spike |
| **分支** | 原 `feature/tool-system-cleanup`；能力已在 **`develop`**（与 **T2-P0-005** 搜索兜底 / 预检等后续提交叠代） |
| **阻塞点** | — |

**目标**：新增一个单入口 `search_files` 只读工具，支持 `target=content|files`，让 LLM 用受权限管控的结构化工具完成内容搜索与文件名 glob 搜索，减少退回 `execute_bash + grep/find`。

**子项**：
- [x] **T-152**：内置 `search_files`（`target=content|files`），对齐 catalog / 权限 / `tool_exec` / `docs/tool-catalog.md` / 集成测试 `tests/search_files_tests.rs`

**依赖**：T2-P0-005、T2-P0-014

**被依赖**：—

**协作接口**：
- 消费：`ToolRegistry`、`PermissionGate`、`PrimitiveExecutor`
- 提供：`search_files`、`docs/tool-catalog.md` 派生条目；架构见 [search_files.md](../../../docs/architecture/tools/search_files.md)

**验收标准（2026-05-08 与实现对齐）**：
- [x] `search_files` 从 catalog 派生到 LLM tool definitions 与 `docs/tool-catalog.md`
- [x] `target=content`：`content` / `files_with_matches` / `count` 等输出形态（见单测与 tool-catalog）
- [x] `target=files`：glob 路径搜索；content-only 字段在 files 目标下忽略
- [x] **缺 `rg`/`fd`**：现网为 **Tier1 缺失时 Tier2（Rust）兜底** + `tomcat chat` 入口可选 **预检/安装指引**（`preflight` / `WIRE_SEARCH_TOOLS_PREFLIGHT`），**不是**初版「仅安装指引、不做 fallback」——以 [search_files.md](../../../docs/architecture/tools/search_files.md) 与 **T2-P0-005** 任务说明为准
- [x] Read 权限与 `path_rules` deny 生效（见 `search_files_tests` deny 用例）
- [x] fmt / clippy / 单测 / 集成测：`search_files_tests` 等已在 `scripts/test-groups.sh` 登记

---
